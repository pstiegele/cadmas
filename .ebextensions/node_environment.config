container_commands:
  01_install_npm_run_all:
    command: "export PATH=$PATH:$NODE_HOME/bin; npm install npm-run-all -g"
  02_npm_install_in_cadmas_client:
      command: "export PATH=$PATH:$NODE_HOME/bin; cd cadmas-client;npm install"
