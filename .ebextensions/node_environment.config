container_commands:
  01_install_npm_run_all:
    command: "export PATH=$PATH:$NODE_HOME/bin; npm install npm-run-all -g"
  02_install_react:
      command: "export PATH=$PATH:$NODE_HOME/bin; npm install react-scripts -g"
