version: 0.2

phases:
  install:
    commands:
      - npm install
      - npm install --prefix cadmas-webclient
  build:
    commands:
      - npm run build --prefix cadmas-webclient

artifacts:
  files:
    - '**/*'
  discard-paths: yes
